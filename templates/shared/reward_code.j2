# Reward

After your code runs, the robot's pose is measured:

    R = -(|h - h*| + |roll - roll*| + |pitch - pitch*|)

where h*, roll*, pitch* are target values. R = 0 is perfect. Higher (closer to 0) is better.